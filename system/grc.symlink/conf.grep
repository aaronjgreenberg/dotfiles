## Intended coloring for ls piped to grep ##
############################################

# Directories
regexp=(^d.*)\d\s(.*$)
colours=default, default, blue
=======

# Symlinks
regexp=(^l.*)\s(.*\s)(->)
colours=default, default, magenta, default
=======

# Executables
regexp=(^-[-rwx]{2,2}x.*)\s(.*$)
colours=default, default, red
